language: node_js
cache:
  directories:
    - node_modules
branches:
  only:
    - master
node_js:
  - "6"
before_install:
  - npm i -g npm@^3.0.0
before_script:
  - npm prune
after_success:
  - '[ "${TRAVIS_PULL_REQUEST}" = "false" ] && npm run  build && npm run deploy
